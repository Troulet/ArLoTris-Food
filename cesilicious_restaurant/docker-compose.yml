version: '3.9'

services:
  cesilicious:
    build: .

  nginx:
    image: nginx:latest
    volumes:
      - ./conf.d:/etc/nginx/conf.d
    depends_on:
      - cesilicious
    ports:
      - 3000:3000